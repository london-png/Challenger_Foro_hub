package med.voll.ForoHub.datos;

/**
 * Grupo de validación personalizado para aplicar reglas específicas solo cuando se valida el campo 'curso'.
 * Útil si decides validar 'curso' condicionalmente (por ejemplo, solo si no es null).
 */
public interface CursoValidationGroup {
}